require 'spec_helper'

describe SlackEmojiList do
  it 'has a version number' do
    expect(SlackEmojiList::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
