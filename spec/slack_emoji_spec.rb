require 'spec_helper'

describe SlackEmoji do
  it 'has a version number' do
    expect(SlackEmoji::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
